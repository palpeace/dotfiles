[user]
    name = {{ .name_self | quote }}
    email = {{ .email_self | quote }}

[core]
    editor = vim

[includeIf "gitdir:~/work/"]
    path = ~/.gitconfig-work

[includeIf "gitdir:~/self/"]
    path = ~/.gitconfig-self
