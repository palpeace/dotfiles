#!/bin/sh
# yazi のプラグインを package.toml の宣言どおりに入れる。
# package.toml hash: {{ include "dot_config/yazi/package.toml" | sha256sum }}
set -eu
PATH="$HOME/.local/share/mise/shims:$PATH"
command -v ya >/dev/null 2>&1 || exit 0
ya pkg install
